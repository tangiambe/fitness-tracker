import React, { useEffect,useState } from 'react'
import { back } from '../../redux/page'
import { useDispatch, useSelector } from 'react-redux'
import { info } from '../../redux/userSlice'



const StepFour = () => {
  
  return (
    <div>
    <h2>Step 4</h2>
    <p>Description.</p>
  </div>
  );
};

export default StepFour